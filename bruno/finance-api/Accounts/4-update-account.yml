info:
  name: Update Account
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3001/api/accounts/{{accountId}}
  params:
    - name: accountId
      value: account-id-here
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Updated Account Name",
        "currency": "USD"
      }
  auth: inherit
